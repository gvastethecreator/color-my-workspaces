# Security

This extension writes workspace settings only (`workbench.colorCustomizations` and `workspaceColor.*`). It does not send data off the machine.

Report vulnerabilities through a [private GitHub security advisory](https://github.com/gvastethecreator/vscode-color-my-workspaces/security/advisories/new). Do not open a public issue with exploit details.
